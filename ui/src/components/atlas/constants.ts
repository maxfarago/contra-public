export const AGE_DOMAIN: [number, number] = [0, 10800]; // 180 minutes (3 hours) in seconds
export const MCAP_DOMAIN: [number, number] = [0, 500]; // 0-500 SOL
export const MIN_AGE_VIEW = 60; // 60 seconds minimum zoom window
export const RENDER_OFFSET = 50; // The offset used in getPosition
export const PADDING_X = 300; // World space padding around the data domain
export const PADDING_Y = 25; // World space padding around the data domain
export const MIGRATION_THRESHOLD_SOL = 410.88;

